# 2026-04-22 작업 이력 — TODO-001/002 수집 원본·매핑 결과 조회 화면 추가

## 변경 요약

| 항목 | 내용 |
|---|---|
| 변경일자 | 2026-04-22 |
| 작업자 | leedh |
| 브랜치 | leedh |
| 참조 TODO | todo-001_수집데이터원본조회.md / todo-002_매핑데이터조회.md |

---

## 변경 상세

### 1. TODO-001: 수집 원본 조회 기능 추가

데이터소스 상세 페이지에 **"수집 원본"** 탭 추가 — `landing_records` 테이블 조회.

#### 백엔드

| 파일 | 유형 | 설명 |
|---|---|---|
| `icon-api/.../datasource/adapter/in/web/LandingRecordController.java` | 신규 | `GET /api/v1/data-sources/{id}/landing-records` |
| `icon-api/.../datasource/adapter/in/web/JsonFilterParam.java` | 신규 | `key:op:value` 파싱 — 보안 검증(키 정규식) 포함 |
| `icon-api/.../datasource/adapter/out/persistence/repository/LandingRecordQueryRepository.java` | 신규 | 네이티브 쿼리 — JSONB 동적 필터, 통계 요약(CASE SUM) |
| `icon-api/.../datasource/application/service/LandingRecordService.java` | 신규 | 기본값 처리(24h), 페이지 범위 제한 |
| `icon-api/.../datasource/dto/LandingRecordDto.java` | 신규 | `PageResponse`, `Item`, `Summary` |

**지원 파라미터**: `startDate`, `endDate`, `ingestionStatus`(반복), `custNo`, `jsonFilters`(반복, `key:op:value`)

#### 프론트엔드

| 파일 | 유형 | 설명 |
|---|---|---|
| `icon-frontend/src/components/datasource/JsonFilterBuilder.tsx` | 신규 | JSONB 동적 필터 행 UI (TODO-002와 공유) |
| `icon-frontend/src/components/datasource/LandingRecordView.tsx` | 신규 | 필터+통계카드+테이블+상세모달 |
| `icon-frontend/src/app/data-sources/api.ts` | 수정 | `fetchLandingRecords()`, `LandingRecord` 타입 추가 |
| `icon-frontend/src/app/data-sources/[id]/page.tsx` | 수정 | `"landing"` 탭 추가 |

---

### 2. TODO-002: 매핑 결과 조회 기능 추가

데이터소스 상세 페이지에 **"매핑 결과"** 탭 추가 — `mapped_storages` 테이블 조회.

> `mapped_storages`에는 `data_source_id`가 없어 `landing_records` JOIN 필수.

#### 백엔드

| 파일 | 유형 | 설명 |
|---|---|---|
| `icon-api/.../datasource/adapter/in/web/MappedStorageController.java` | 신규 | `GET /api/v1/data-sources/{id}/mapped-storages`, `GET /api/v1/data-sources/mapped-storages/{id}/with-origin` |
| `icon-api/.../datasource/adapter/out/persistence/repository/MappedStorageQueryRepository.java` | 신규 | landing_records JOIN, JSONB 동적 필터, 원본 비교 단건 조회 |
| `icon-api/.../datasource/application/service/MappedStorageService.java` | 신규 | 기본값 처리, 페이지 범위 제한 |
| `icon-api/.../datasource/dto/MappedStorageDto.java` | 신규 | `PageResponse`, `Item`, `Summary`, `DetailWithOrigin` |

**지원 파라미터**: `startDate`, `endDate`, `processingStatus`(반복), `transactionId`, `custNo`, `jsonFilters`(반복)

#### 프론트엔드

| 파일 | 유형 | 설명 |
|---|---|---|
| `icon-frontend/src/components/datasource/MappedStorageView.tsx` | 신규 | 필터+통계카드+테이블+상세모달(탭2개: 매핑결과/원본비교) |
| `icon-frontend/src/app/data-sources/api.ts` | 수정 | `fetchMappedStorages()`, `fetchMappedStorageWithOrigin()`, 관련 타입 추가 |
| `icon-frontend/src/app/data-sources/[id]/page.tsx` | 수정 | `"mapped"` 탭 추가 |

---

### 3. 파서 기능 재설계 프론트엔드 완성 (이전 작업 연속)

| 파일 | 유형 | 설명 |
|---|---|---|
| `icon-frontend/src/app/parsers/api.ts` | 수정 | `sourceField`, `configJson` 추가, `targetStandardFieldId` 제거, `LinkParserRequest`/`LinkedParserResponse` 추가, 데이터소스-파서 연결 API 함수 추가 |
| `icon-frontend/src/app/parsers/page.tsx` | 재설계 | sourceField 입력, 타입별 UI (DELIMITER: 구분자+필드명, FIXED_WIDTH: byteLength+필드명, REGEX: pattern+group+필드명), 펼침 미리보기 개선 |
| `icon-frontend/src/components/datasource/DataSourceParserSection.tsx` | 신규 | 데이터소스-파서 연결/해제 UI |
| `icon-frontend/src/components/datasource/OriginalSchemaView.tsx` | 수정 | 파서 컬럼·핸들러 전면 제거 (파서는 데이터소스 레벨로 이동) |
| `icon-frontend/src/app/data-sources/api.ts` | 수정 | `DataSourceOriginalSchema`·`StandardFieldMappingRequest`에서 `parserId` 제거 |
| `icon-frontend/src/app/data-sources/[id]/page.tsx` | 수정 | `"parsers"` 탭 추가 (DATABASE 타입 미표시), `DataSourceParserSection` 렌더 |

---

### 4. DB 성능 인덱스 추가

| 파일 | 설명 |
|---|---|
| `db/migrations/V1_0_11__add_mapped_storages_performance_indexes.sql` | `mapped_storages` reg_dt·landing_record_id·processing_status·transaction_id 인덱스 |
| `icon-api/src/main/resources/db/migrations/V1_0_11__*.sql` | 동일 파일 복사 |

---

## 탭 최종 순서 (`/data-sources/[id]`)

```
개요 | 연결 설정 | 원본 필드 | 프로파일 관리 | 파서 설정 | 수집 원본 | 매핑 결과
```

---

## 빌드 검증

- 백엔드: `./gradlew :icon-api:bootJar` — **BUILD SUCCESSFUL**
- 프론트엔드: `tsc --noEmit` — **오류 없음** (테스트 파일 제외)
