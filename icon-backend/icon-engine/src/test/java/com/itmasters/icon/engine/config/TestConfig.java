package com.itmasters.icon.engine.config;

import org.springframework.boot.test.context.TestConfiguration;

/**
 * 테스트 전용 설정
 */
@TestConfiguration
public class TestConfig {
    // icon-engine 자체 DerivedFieldService 사용으로 별도 설정 불필요
}
